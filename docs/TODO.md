# TODO

- Windows To Go.
- Rufus's remaining Windows options: silent install, S mode, the 2023
  bootloaders, SkuSiPolicy.
- Translations (the window is English only).
- A D-Bus backend for UDisks2 instead of `udisksctl`.
